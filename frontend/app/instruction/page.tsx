import InstructionPage from "@/src/screens/instruction";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Как заказать",
};

function Page() {
  return <InstructionPage />;
}

export default Page;
